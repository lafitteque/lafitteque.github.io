import Hideable from '@components/Hideable.astro'
import Remarque from '@components/Remarque.astro'
import Exercice from '@components/Exercice.astro'
import Item from '@components/Item.astro'
import Indent from '@components/Indent.astro'
import Fil from '@components/Fil.astro'

import Prod from "@components/Geogebra/produit_complexes.astro"
import Somme from "@components/Geogebra/somme_complexes.astro"


<Item type='definition' titre='les nombres complexes' searchDesc="forme algébrique affixe">

On appelle $\C$ l'ensemble des nombres s'écrivant sous la forme $a+ib$ où $(a,b) \in \R^2$ et où $i$ est un nombre tel que $i^2 = -1$. 

Si $z = a + ib\v (a\v b) \in \R^2$ on dit que $a + ib$ est la forme algébrique de $z$

<br/>

On peut représenter les éléments de $\C$ dans le repère orthonormé $\big(O \v \vec{i} \v \vec{j} \big)$ par le point de coordonnées $\Point{a}{b}$. 

On dit que le complexe $z = a + ib$ a pour **affixe** $\Point{a}{b}$.
</Item>


<Item type='definition' titre="partie réelle et imaginaire d'un nombre complexe">

Soit $z \in \C$ et $z = a + ib$ sa forme algébrique. On note :

$\cd$ $\Re(z)$ la quantité $a$ qui est appelée "partie réelle de $z$"

$\cd$ $\Im(z)$ la quantité $b$ qui est appelée "partie imaginaire de $z$"

Il suit que $z = \Re(z) + i\Im(z)$. Si $z \in \C^*$ et $\Re(z) = 0$, on dit que $z$ est un **imaginaire pur** 
</Item>

<Item type='definition' titre='opérations entre complexes' searchDesc="addition multiplication produit somme">
Soient $z = a+ib$ et $z' = c + id$ deux nombres complexes et leur forme algébrique. 

On définit leur somme et leur produit comme suit :

$\cd$ $z + z' = (a+c) + i(b+d)$

$\cd$ $z \times z' = (a+ib)(c+id) = ac + iad + ibc - bd = (ac - bd) + i(ad + bc)$
</Item>


**Illustrations :**

<Fil>
Ci-dessous, on utilise Géogébra pour illustrer le fonctionnement de la somme :

$$
z_1 + z_2 = z_3
$$

Déplacez $z_1$ et $z_2$ pour observer comment le résultat $z_3$ varie.

<Somme></Somme>

En particulier, on peut faire le lien avec la somme de vecteurs. La somme de nombres complexes est en fait la translation d'un point.

<br/>

<br/>

On illustre maintenant le fonctionnement du produit :

$$
z_1 \times z_2 = z_3
$$

Déplacez $z_1$ et $z_2$ pour observer comment le résultat $z_3$ varie.

<Prod></Prod>

Le produit est plus riche que la somme et plus intéressant. On remarque qu'il permet de traduire une notion de rotation en plus de multiplier les longueurs des deux nombres complexes de départ.

Il s'agit là de toute la puissance des complexes : modéliser des phénomènes d'oscillations et des phénomènes périodiques.

<br/>

Malgré l'apparence très abstraite des nombres complexes, ses applications sont extrêmement nombreuses et très concètes. De l'étude des photons jusqu'aux comportements de circuits électriques, les nombres complexes sont des outils à ne pas négliger.

</Fil>


<Remarque titre=''>
Ces opérations sont assez naturelles : l'addition est faite terme à terme en utilisant l'écriture $z = a + ib$ et $z' = c + id$ et le produit est exactement le résultat que l'on attendrait avec une double distributivité. 

<br/>

On remarque qu'il s'agit bien là d'une définition. On ne peut nullement déduire cela d'une double distributivité puisque cela supposerait que, pour définir le produit de complexes, on sait déjà faire le produit de complexes. On appelle ce genre de raisonnement un "raisonnement circulaire" qu'il faut à tout prix éviter.
</Remarque>

<Exercice titre=''>

a\) Simplifier $12 - 27 i + 14 -5 \times 3i$.

b\) Développer et simplifier $3i(4 -  7i) + (1 + i)(1-i)$

c\) Compléter l'identité remarquable suivante : $a^2 + b^2 = (\dotsm) \times (\dotsm)$ où $a \v b$ sont des complexes.

</Exercice>

<Hideable type='solution' titre=''>

a\)  $12 - 27 i + 14 -5 \times 3i = 12 - 27i + 14 -15i$

Puis on simplifie : $12 - 27 i + 14 -5 \times 3i = 26 - 42i$

<br/>

b\) On développe : $3i(4 -  7i) + (1 + i)(1-i) = 12i - 21 i \times i + 1 - i + i - i \times i$

On rappelle que $i \times i = -1$. On a donc $3i(4 -  7i) + (1 + i)(1-i) = 12i + 21 + 1 -i + i + 1$

On simplifie et on obtient $3i(4 -  7i) + (1 + i)(1-i) = 23 + 12i$

<br/>

c\) $a^2 + b^2 = a^2 - (ib)^2 = (a+ib)(a-ib)$. On pouvait le remarquer dans le calcul de la question b\) puisque $(1 + i)(1-i) = 1^2 + 1^2$.

</Hideable>