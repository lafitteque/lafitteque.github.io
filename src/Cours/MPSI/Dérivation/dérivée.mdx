import Hideable from '@components/Hideable.astro'
import Remarque from '@components/Remarque.astro'
import Exercice from '@components/Exercice.astro'
import Item from '@components/Item.astro'
import Indent from '@components/Indent.astro'
import Fil from '@components/Fil.astro'




<Item type='definition' titre='tangente à la courbe'  searchDesc="fonction droite">

Si $f$ est dérivable en $a \in I$, on appelle tangente à la courbe représentative $\cC_f$ ( ou simplement à $f$) en $a$ la droite d'équation :

$$
y = f'(a)(x-a) + f(x)
$$ 
</Item>


<Remarque titre=''>

 localement, la tangente est une approximation de la courbe. 
Cela peut être très utile en physique par exemple pour l'étude d'oscillations avec l'approximation quand $x$ est proche de $0$ : $sin(x) \approx x$.

On verra de meilleures approximations par des polynômes de plus haut degré plus tard dans l'année.
</Remarque>

